## 2026-06-07T09:13:30Z
You are the Worker for Milestone 2.
Your working directory is d:\sahma\Documents\GitHub\Systemoptimierungs_Windows\.agents\worker_m2.

Your task is to modify d:\sahma\Documents\GitHub\Systemoptimierungs_Windows\Services\RecoveryService.cs according to the following requirements:

1. Sector Search Loop buffer bounds fix:
   - In `ScanPhysicalSectorsAsync` (around line 364), pass `(int)bytesRead` as the `validBytes` parameter when calling `MatchCarvingSignatureOffset`.
   - Update `MatchCarvingSignatureOffset` signature to:
     `private string? MatchCarvingSignatureOffset(byte[] block, int offset, int validBytes, bool includeDocs, bool includeImages, bool includeVideos, bool includeMusic)`
   - Inside `MatchCarvingSignatureOffset`, calculate remaining bytes using `validBytes`:
     `int remaining = validBytes - offset;`
   - Update `IsAsciiSector` signature to accept remaining bytes:
     `private bool IsAsciiSector(byte[] block, int offset, int remaining)`
     Inside `IsAsciiSector`, calculate the limit using `remaining` instead of `block.Length`:
     `int limit = Math.Min(remaining, 512);`
     In `MatchCarvingSignatureOffset`, call it as `IsAsciiSector(block, offset, remaining)`.

2. Support raw carving of `.lnk` files:
   - When `includeDocs` is true, check for Shell Link `.lnk` magic signature:
     - Check: `block[offset] == 0x4C && block[offset + 1] == 0x00 && block[offset + 2] == 0x00 && block[offset + 3] == 0x00`
     - Verify CLSID structure by ensuring `remaining >= 20` and the following bytes match:
       `block[offset + 4] == 0x01 && block[offset + 5] == 0x14 && block[offset + 6] == 0x02 && block[offset + 7] == 0x00 && block[offset + 16] == 0xC0 && block[offset + 19] == 0x46`
     - If matched, return `".lnk"`.

3. Support format distinction for plain-text formats:
   - When `IsAsciiSector(block, offset, remaining)` is true, instead of just returning `".txt"`, inspect the first `Math.Min(remaining, 128)` bytes. Decode them as an ASCII string.
   - If the string starts with or contains C# keywords (`using System`, `namespace `, `public class `, `//`, `/*`), return `".cs"`.
   - If it starts with `{` or `[`, return `".json"`.
   - If it starts with `<!DOCTYPE html` or `<html` (case-insensitive), return `".html"`.
   - If it contains log identifiers like `[INFO]`, `[WARN]`, `[ERROR]`, or `[DEBUG]`, return `".log"`.
   - Otherwise, return `".txt"`.

4. Implement `includeMusic` checks:
   - If `includeMusic` is true, check for:
     - MP3: ID3 header `block[offset] == 0x49 && block[offset + 1] == 0x44 && block[offset + 2] == 0x33` OR frame header `block[offset] == 0xFF && block[offset + 1] == 0xFB`
     - WAV: RIFF header `block[offset] == 0x52 && block[offset + 1] == 0x49 && block[offset + 2] == 0x46 && block[offset + 3] == 0x48` and `remaining >= 12` and sub-chunk format `block[offset + 8] == 0x57 && block[offset + 9] == 0x41 && block[offset + 10] == 0x56 && block[offset + 11] == 0x45`
     - FLAC: header `block[offset] == 0x66 && block[offset + 1] == 0x4C && block[offset + 2] == 0x61 && block[offset + 3] == 0x63`
     - OGG: header `block[offset] == 0x4F && block[offset + 1] == 0x67 && block[offset + 2] == 0x67 && block[offset + 3] == 0x53`
     - Return the corresponding extensions (`.mp3`, `.wav`, `.flac`, `.ogg`).

5. Update `EstimateCarvedFileSizeFromOffset`:
   - Map the newly added extensions to reasonable size estimations:
     - `.cs` -> `50000` (50 KB)
     - `.json` -> `100000` (100 KB)
     - `.html` -> `200000` (200 KB)
     - `.log` -> `1000000` (1 MB)
     - `.lnk` -> `4096` (4 KB)
     - `.pptx` -> `2000000` (2 MB)
     - `.txt` -> `100000` (100 KB)
     - `.mp3` -> `6000000` (6 MB)
     - `.wav` -> `30000000` (30 MB)
     - `.flac` -> `20000000` (20 MB)
     - `.ogg` -> `5000000` (5 MB)

6. Run the build command (`dotnet build`) to ensure the project compiles with 0 errors.

You can reference the patch generated by Explorer 3 at: `d:\sahma\Documents\GitHub\Systemoptimierungs_Windows\.agents\explorer_m2_3\RecoveryService.cs.patch`

MANDATORY INTEGRITY WARNING:
DO NOT CHEAT. All implementations must be genuine. DO NOT hardcode test results, create dummy/facade implementations, or circumvent the intended task. A Forensic Auditor will independently verify your work. Integrity violations WILL be detected and your work WILL be rejected.

Document your changes and build/test verification details in `d:\sahma\Documents\GitHub\Systemoptimierungs_Windows\.agents\worker_m2\changes.md`. Provide a handoff.md report summarizing your work and results.
